language: node_js

node_js:
  - 10.19.0

env:
  - DATABASE_URL=postgresql://postgres@localhost:5432/greet


  before_script:
  - psql -c 'create database greet;' -U postgres
  - psql -U postgres -d greet -a -f users.sql

  services:
  - postgresql
  